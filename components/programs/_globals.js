/*
  This folder contains container components. 

  Such components don't interface with the Store. 
  Instead, they're passed data, and render it as HTML tags. 

  Display components are used within Object components.
*/
import box from './box.vue';
import program from './program.vue';



export default {
  box,
  program,
};