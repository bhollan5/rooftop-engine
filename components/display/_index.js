/*
  This folder contains "renderer" components. 

  Such components don't interface with the Store. 
  Instead, they're passed data, and render it as HTML tags. 

  Terminal components are used within Object components.
*/
import text from './text.vue';



export default {
  text,
};